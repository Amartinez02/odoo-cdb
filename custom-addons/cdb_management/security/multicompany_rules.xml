<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- ═══════════════════════════════════════════════════════════
             Multi-Company Record Rules — cdb_management
             ═══════════════════════════════════════════════════════════ -->

        <!-- church.ministry -->
        <record id="rule_church_ministry_multicompany" model="ir.rule">
            <field name="name">Church Ministry: multi-company</field>
            <field name="model_id" ref="model_church_ministry"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.role -->
        <record id="rule_church_role_multicompany" model="ir.rule">
            <field name="name">Church Role: multi-company</field>
            <field name="model_id" ref="model_church_role"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.activity -->
        <record id="rule_church_activity_multicompany" model="ir.rule">
            <field name="name">Church Activity: multi-company</field>
            <field name="model_id" ref="model_church_activity"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.attendance.type -->
        <record id="rule_church_attendance_type_multicompany" model="ir.rule">
            <field name="name">Church Attendance Type: multi-company</field>
            <field name="model_id" ref="model_church_attendance_type"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.attendance -->
        <record id="rule_church_attendance_multicompany" model="ir.rule">
            <field name="name">Church Attendance: multi-company</field>
            <field name="model_id" ref="model_church_attendance"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.attendance.line -->
        <record id="rule_church_attendance_line_multicompany" model="ir.rule">
            <field name="name">Church Attendance Line: multi-company</field>
            <field name="model_id" ref="model_church_attendance_line"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.attendance.report -->
        <record id="rule_church_attendance_report_multicompany" model="ir.rule">
            <field name="name">Church Attendance Report: multi-company</field>
            <field name="model_id" ref="model_church_attendance_report"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.attendance.report.new.line -->
        <record id="rule_church_attendance_report_new_line_multicompany" model="ir.rule">
            <field name="name">Church Attendance Report New Line: multi-company</field>
            <field name="model_id" ref="model_church_attendance_report_new_line"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

        <!-- church.family.relation -->
        <record id="rule_church_family_relation_multicompany" model="ir.rule">
            <field name="name">Church Family Relation: multi-company</field>
            <field name="model_id" ref="model_church_family_relation"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

    </data>
</odoo>
